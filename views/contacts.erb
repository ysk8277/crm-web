<h1>All Contacts</h1>

<ul>
  <% @@rolodex.contacts.each do |contact| %>  
  <li> <%= contact.first_name %> <%= contact.last_name %> <%= contact.email %> <%= contact.notes %> <%= contact.id %> </li>
  <% end %>
</ul>
